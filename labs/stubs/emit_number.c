    /* TODO: v sonini base asosida emit() orqali chiqaring: ishora (neg, +, bo'shliq),
     * kenglik (sp->width), '-' (chapga), '0' (nol bilan to'ldirish), aniqlik (sp->prec).
     * Har bir chiqarilgan belgi uchun (*count)++. */
    (void)emit;
    (void)ctx;
    (void)v;
    (void)neg;
    (void)base;
    (void)upper;
    (void)sp;
    (void)count;
